// Lightweight model to replace the google_maps LatLng object
class PlaceLatLng {
  final double latitude;
  final double longitude;

  PlaceLatLng({required this.latitude, required this.longitude});
}
